__pycache__/
*.pyc
*.egg-info
.DS_Store
*.pem
extension/native-host/.crx-private-key.pem
# repo-local virtualenv + user config (keep .env.example tracked)
/.venv
/.venv/
.env
.env.policy
.env.cuda

# dryforge produce/run artifacts (3-doc, worktrees) — never tracked
.dryforge/
